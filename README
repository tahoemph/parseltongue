This directory contains source for a document that attempts to bootstrap
existing programmers into using Python.  A rough draft of this was
originall written in google docs but then converted to TeX when that
was not sufficient.  The google docs version is left as a historical
artifact as a html file.
